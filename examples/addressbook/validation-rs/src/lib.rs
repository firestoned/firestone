//! The addressbook rules, generated for rust.
//!
//! `cargo test` runs the suite firestone generated from the `examples:` in the
//! resource files, and `cargo run --example resolver` shows a resolver being
//! implemented and the rules rejecting a request.

pub mod validation;
